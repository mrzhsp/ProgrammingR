var1 <- 5
var <- "5"
var3 <- TRUE
var1 == 5
# we can use the command "typeof()" to see what kind of a variable we have.
as.Date("2010-10-08")
var4 <- as.Date("2010-10-08")
var5 <- c(1,2,3)
var5 + 1
1:10
-10:10
1:10 < 5
var6 <- 1:20
var6 - 1:5
# We should pay attention to the length of the vectors that are going to affect each other.
3/2
3L / 2
# We put L to define the number as an integer. 
library(lubridate)
now
now()
lubridate::now()
base::date
